{
  "name": "preact-vite-ts-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "PROD=true tsc && vite build",
    "preview": "tsc && vite build && vite preview",
		"prettify": "npx prettier --write ."
  },
  "dependencies": {
    "preact": "^10.13.1",
    "preact-router": "^4.1.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "@types/node": "^18.16.3",
    "prettier": "2.8.8",
    "typescript": "^5.0.2",
    "vite": "^4.3.2"
  }
}
