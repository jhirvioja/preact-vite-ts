{
  "name": "preact-vite-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prettier": "npx prettier --write .",
    "build": "PROD=true tsc && vite build",
    "preview": "tsc && vite build && vite preview"
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "preact": "^10.13.1",
    "preact-router": "^4.1.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "@types/node": "^18.16.3",
    "prettier": "2.8.8",
    "typescript": "^5.0.2",
    "vite": "^4.3.2"
  }
}
